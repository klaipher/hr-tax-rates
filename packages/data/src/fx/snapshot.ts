import Decimal from 'decimal.js'
import type { IsoDate } from './exchange-rate.ts'

/**
 * Останній відлік курсу EUR/UAH, зафіксований у репозиторії.
 *
 * Потрібен, бо застосунок статичний і мусить показати число навіть тоді, коли
 * НБУ недоступний або користувач офлайн. Джерело те саме, що й для живого
 * запиту, — відкрите API НБУ, див. `nbu.ts`.
 *
 * Дата тут не прикраса: саме вона відрізняє «курс НБУ» від «курс НБУ
 * піврічної давнини», і саме її показують поруч із числом.
 *
 * TODO: оновлюється вручну. Скрипт, що перезаписує снепшот на етапі збірки —
 * як `scripts/extract-hok-fixtures.ts` для фікстур HOK — ще не написано.
 */
export const NBU_EUR_UAH_SNAPSHOT = {
  value: new Decimal('51.5372'),
  asOf: '2026-08-05',
} as const satisfies { value: Decimal; asOf: IsoDate }
