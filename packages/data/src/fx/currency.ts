/**
 * Код валюти за ISO 4217.
 *
 * Валюта — окремий тип, а не рядок: сума без валюти і курс без пари валют
 * однаково безглузді, а «просто рядок» дозволяє переплутати їх мовчки.
 *
 * Рушій має власний, ширший `Money<C>` із валютою в типі суми. Тут його немає
 * навмисно: `packages/data` не залежить від `packages/engine` — залежність
 * спрямована в інший бік, — а другий, структурно однаковий `Money`, який
 * доводилося б тримати синхронним із першим, гірший за проблему, яку він
 * розв'язує.
 */
export type CurrencyCode = 'EUR' | 'UAH'
